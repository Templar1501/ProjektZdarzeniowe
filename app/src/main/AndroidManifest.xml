<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projektzdarzeniowe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controler.AddOrRemoveVehicleActivity"></activity>
        <activity android:name=".Controler.ReturnVehicleActivity" />
        <activity android:name=".Controler.TakeVehicleActivity" />
        <activity android:name=".Controler.OrderActivity" />
        <activity android:name=".Controler.DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controler.RegisterActivity" />
        <activity android:name=".Controler.LoginActivity" />
        <activity android:name=".Controler.MainActivity" />
    </application>

</manifest>